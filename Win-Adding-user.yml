- name: Adding the user to the Windows system
  hosts: all
  tasks: 
   - name: Adding the user to the system
     win_user:
      name: redhat
      password: RedHat1!
      state: present
      groups:
       - Users
       - Developers
      groups_action: replace
